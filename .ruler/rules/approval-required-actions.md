# Approval Required Actions

Ask explicit user approval before push, pull, delete, install, unknown scripts, global config changes, secrets changes, GitHub repo changes, deploys, and billing/account changes.
